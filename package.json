{
  "name": "ninthtool",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "This file exists to set type=module so the .js files under src and tests are ES modules for Node exactly as they already are for the browser. It declares no dependencies and never will. Nothing is installed, there is no lock file, and no CI step runs a package manager.",
  "scripts": {
    "test": "node --test tests/unit",
    "style": "node scripts/check_style.mjs",
    "readiness": "node scripts/readiness.mjs"
  }
}
